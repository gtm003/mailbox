### Mailbox [(Deploy)](https://gtm003.github.io/mailbox/)

* clone the repo `git clone git@github.com:gtm003/mailbox.git`
* install dependencies `npm i`
* runs the app in the development mode `npm start`
